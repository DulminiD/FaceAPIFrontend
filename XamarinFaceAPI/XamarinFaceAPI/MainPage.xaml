<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamarinFaceAPI"
             x:Class="XamarinFaceAPI.MainPage">

    <StackLayout>
        <Label Text="Enter the url"/>
        <Entry Text="{Binding FaceAttributes.URL}"/>

        <ListView ItemsSource="{Binding ListOfItems}">
          
        </ListView>
        <Button Text="Get Attributes" Command="{Binding ButtonClicked}" BackgroundColor="Accent"/>
    </StackLayout>

</ContentPage>
